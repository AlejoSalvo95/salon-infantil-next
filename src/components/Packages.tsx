import { money, packageDisplayName, packages, type PackageName } from "@/lib/data";
export function Packages({ onSelect }: { onSelect: (name: PackageName) => void }) {
  return <section className="packages section" id="programs"><div className="section-head"><div><p className="kicker coral">Choose a growing experience</p><h2>A garden session<br/><em>made for you.</em></h2></div><p>Every program includes private garden access, guided activities, and all materials. Just bring your curiosity.</p></div><div className="package-grid">{packages.map(pkg=><article className={`package ${pkg.popular?"popular":""}`} key={pkg.name}>{pkg.popular&&<div className="badge">Most loved</div>}<div className="package-top"><span>{pkg.eyebrow}</span><h3>{packageDisplayName(pkg.name)}</h3><p className="price"><small>from</small> {money(pkg.price)}</p></div><ul>{pkg.features.map(x=><li key={x}>{x}</li>)}</ul><button onClick={()=>onSelect(pkg.name)}>Choose {packageDisplayName(pkg.name)} <span>→</span></button></article>)}</div></section>;
}
