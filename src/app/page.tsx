"use client";
import { useState } from "react";
import { Header } from "@/components/Header";import { Hero } from "@/components/Hero";import { Packages } from "@/components/Packages";import { Calculator } from "@/components/Calculator";import { BookingModal } from "@/components/BookingModal";import type { Extra,PackageName } from "@/lib/data";
export default function Home(){const[booking,setBooking]=useState(false);const[pkg,setPkg]=useState<PackageName>("Nube");const[kids,setKids]=useState(20);const[extras,setExtras]=useState<Extra[]>([]);const select=(x:PackageName)=>{setPkg(x);setBooking(true)};return <><Header onBook={()=>setBooking(true)}/><main><Hero onBook={()=>setBooking(true)}/><Space/><Packages onSelect={select}/><Calculator kids={kids} setKids={setKids} selectedExtras={extras} setExtras={setExtras} packageName={pkg} setPackage={setPkg} onBook={()=>setBooking(true)}/><Testimonials/></main><Footer/><BookingModal open={booking} onClose={()=>setBooking(false)} packageName={pkg} setPackage={setPkg} kids={kids} setKids={setKids} extras={extras}/></>}
function Space(){return <section className="space section" id="espacio"><div className="section-head"><div><p className="kicker coral">A garden made for discovery</p><h2>Plant, explore,<br/>and <em>get curious.</em></h2></div><p>Safe, bright spaces designed for young growers. Every corner invites a new botanical discovery.</p></div><div className="feature-grid"><Feature kind="big" art="playground" icon="★" number="01" title="Discovery greenhouse">Hands-on planting stations, sensory beds, and safe spaces for botanical exploration.</Feature><Feature art="workshop" icon="✿" number="02" title="Botanical workshop">A place to plant, create, and take home something they nurtured themselves.</Feature><Feature art="grownups" icon="☕" number="03" title="Garden lounge">Fresh coffee, comfortable seating, and a full view of the growing space.</Feature></div></section>}
function Feature({kind="",art,icon,number,title,children}:{kind?:string;art:string;icon:string;number:string;title:string;children:React.ReactNode}){return <article className={`feature ${kind}`}><div className={`art ${art}`}>{icon}</div><div><small>{number}</small><h3>{title}</h3><p>{children}</p></div></article>}
function Testimonials(){return <section className="testimonials section" id="opiniones"><p className="kicker coral">Happy growers</p><blockquote>“My daughter came home excited about every seed she planted. The space was beautiful, thoughtful, and cared for down to the last detail.”</blockquote><div className="quote-author"><div className="avatar">MC</div><span><strong>María Clara</strong><small>Olivia's mother · age 6</small></span><div className="stars">★★★★★</div></div></section>}
function Footer(){return <footer><a className="logo" href="#inicio"><span>☁</span> nube</a><p>Helping curiosity take root.</p><div><a className="plants-footer-link" href="/plants"></a></div><small>© 2026 Nube Garden · Demo site</small></footer>}
